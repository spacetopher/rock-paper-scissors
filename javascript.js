// Randomly return 'Rock', 'Paper' or 'Scissors'
function getComputerChoice() {
    const options = ["Rock", "Paper", "Scissors"];
    const randChoice = options[Math.floor(Math.random()*options.length)]
    console.log(randChoice);
}


// Write a function that plays a single round of Rock Paper Scissors
function playRound(playerSelection, computerSelection) {
    // getPlayerChoice()
    prompt = "Rock, Paper or Scissors?"

    // Establish values of choices, make a function to compare them.

     //  compare values 
   if getComputerChoice > getComputerChoice
   then 
   console.log ("You won!, _ beats _")
   else
   console.log ("You lose, _ beats _")
  }


